version: '3.8'
services:
  backend:
    build: .
    ports:
      - "4000:4000"
    volumes:
      - ./src:/app/src
      - ./products.json:/app/products.json
    environment:
      - NODE_ENV=development
    command: npm run dev
    restart: unless-stopped
